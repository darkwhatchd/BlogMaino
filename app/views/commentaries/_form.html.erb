<%= simple_form_for [@post, @post.commentaries.build] do |f| %>
  <div class='field'>
    <div class="control">
      <%= f.text_area :body, class: 'textarea', value: 'Escreva seu comentário aqui' %>
    </div>
  </div>
  <div class="columns">
    <div class="column is-3 is-offset-4">
      <div class='actions'>
        <%= f.button :submit, class: 'button is-info commentary-button' %>
      </div>
    </div>
  </div>
<% end %>
